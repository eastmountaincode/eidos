{
  "name": "eidos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:portal": "rm -rf dist && mkdir -p dist && cp -R apps/portal/. dist/ && find dist -name '._*' -type f -delete",
    "build": "npm run build:portal"
  }
}
