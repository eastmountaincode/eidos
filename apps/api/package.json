{
  "name": "eidos-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "schema:remote": "wrangler d1 execute eidos --remote --file ./schema.sql",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "miniflare": "^4.20260601.0",
    "wrangler": "^4.97.0"
  }
}
