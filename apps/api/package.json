{
  "name": "eidos-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "schema:remote": "wrangler d1 execute eidos --remote --file ./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  }
}
